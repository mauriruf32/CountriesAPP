const Card = (props) =>{
    return(
        <div>
        <p>bander:{props.flag}</p>
         <p>Nombre:{props.name}</p>
        <p>Email:{props.continent}</p>
        </div>
    )
}

export default Card;